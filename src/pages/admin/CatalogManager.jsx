import React, { useCallback, useEffect, useState } from "react";
import { Package, Pencil, Plus, Trash2, Users, X } from "lucide-react";
import ImageUpload from "../../components/ui/ImageUpload.jsx";
const productBlank = { name: "", description: "", product_type: "trial", price: "0", currency: "VND", specifications: "", status: "published" };
const teamBlank = { name: "", title: "", bio: "", avatar_url: "", profile_intro: "", skills: "", experience: "", featured_projects: "", articles: "", sort_order: "0", status: "active" };
function TeamFields({ form, update }) { const changeAvatar = (url) => update("avatar_url")({ target: { value: url } }); return <><label>Họ và tên<input required value={form.name} onChange={update("name")} /></label><label>Vai trò<input required value={form.title} onChange={update("title")} /></label><label className="admin-form-full">Giới thiệu ngắn<textarea rows="3" value={form.bio} onChange={update("bio")} /></label><label className="admin-form-full">Giới thiệu profile / câu chuyện cá nhân<textarea rows="5" value={form.profile_intro} onChange={update("profile_intro")} /></label><div className="admin-form-full"><span className="form-label">Ảnh đại diện</span><ImageUpload value={form.avatar_url} onChange={changeAvatar} label="Tải ảnh đại diện" /></div><label>Kỹ năng, ngôn ngữ (cách nhau dấu phẩy)<input value={form.skills} onChange={update("skills")} placeholder="React, Node.js, Figma" /></label><label className="admin-form-full">Kinh nghiệm theo giai đoạn (mỗi dòng một mục)<textarea rows="4" value={form.experience} onChange={update("experience")} /></label><label className="admin-form-full">Dự án tiêu biểu (mỗi dòng một dự án)<textarea rows="3" value={form.featured_projects} onChange={update("featured_projects")} /></label><label>Thứ tự hiển thị<input required type="number" min="0" value={form.sort_order} onChange={update("sort_order")} /></label><label>Trạng thái<select value={form.status} onChange={update("status")}><option value="active">Hiển thị</option><option value="inactive">Ẩn</option></select></label></>; }
export default function CatalogManager({ kind }) { const product = kind === "products", endpoint = product ? "/api/products" : "/api/team", Icon = product ? Package : Users; const [items, setItems] = useState([]), [form, setForm] = useState(product ? productBlank : teamBlank), [editing, setEditing] = useState(null), [open, setOpen] = useState(false), [error, setError] = useState(""), [saving, setSaving] = useState(false); const load = useCallback(() => fetch(endpoint, { credentials: "include" }).then((r) => r.json()).then((b) => setItems(b.data || [])).catch(() => setError("Không tải được dữ liệu.")), [endpoint]); useEffect(() => { load(); }, [load]); const update = (field) => (e) => setForm((v) => ({ ...v, [field]: e.target.value })); const create = () => { setForm(product ? productBlank : teamBlank); setEditing(null); setOpen(true); }; const edit = (item) => { setEditing(item.id); setForm(product ? { name: item.name, description: item.description || "", product_type: item.product_type, price: String(item.price), currency: item.currency, specifications: item.specifications.join(", "), status: item.status } : { name: item.name, title: item.title, bio: item.bio || "", avatar_url: item.avatar_url || "", profile_intro: item.profile_intro || "", skills: (item.skills || []).join(", "), experience: (item.experience || []).join("\n"), featured_projects: (item.featured_projects || []).join("\n"), articles: (item.articles || []).join("\n"), sort_order: String(item.sort_order), status: item.status }); setOpen(true); }; const save = async (e) => { e.preventDefault(); const lines = (x) => x.split("\n").map((v) => v.trim()).filter(Boolean); const payload = product ? { ...form, price: Number(form.price), currency: form.currency.toUpperCase(), specifications: form.specifications.split(",").map((v) => v.trim()).filter(Boolean) } : { ...form, sort_order: Number(form.sort_order), skills: form.skills.split(",").map((v) => v.trim()).filter(Boolean), experience: lines(form.experience), featured_projects: lines(form.featured_projects), articles: lines(form.articles) }; setSaving(true); try { const r = await fetch(editing ? `${endpoint}/${editing}` : endpoint, { method: editing ? "PATCH" : "POST", credentials: "include", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) }), b = await r.json(); if (!r.ok) throw new Error(b.error || b.errors?.[0]); setOpen(false); load(); } catch (x) { setError(x.message || "Không thể lưu."); } finally { setSaving(false); } }; const remove = async (id) => { if (!window.confirm("Xóa mục này?")) return; await fetch(`${endpoint}/${id}`, { method: "DELETE", credentials: "include" }); load(); }; const label = product ? "Sản phẩm" : "Đội ngũ"; return <div><div className="admin-toolbar"><h2 className="admin-section-title">{label} ({items.length})</h2><button className="btn-primary admin-add" onClick={create}><Plus size={17} />Thêm {product ? "sản phẩm" : "thành viên"}</button></div>{error && <p className="form-error">{error}</p>}{open && <form className="admin-form" onSubmit={save}><div className="admin-form-header"><h3>{editing ? "Chỉnh sửa" : "Thêm mới"} {label.toLowerCase()}</h3><button className="icon-btn" type="button" onClick={() => setOpen(false)}><X size={17} /></button></div><div className="admin-form-grid">{product ? <><label>Tên sản phẩm<input required value={form.name} onChange={update("name")} /></label><label>Loại<select value={form.product_type} onChange={update("product_type")}><option value="trial">Trải nghiệm</option><option value="sale">Bán</option></select></label><label className="admin-form-full">Mô tả<textarea rows="3" value={form.description} onChange={update("description")} /></label><label>Thông số<input value={form.specifications} onChange={update("specifications")} /></label><label>Giá<input required type="number" value={form.price} onChange={update("price")} /></label></> : <TeamFields form={form} update={update} />}</div><button className="btn-primary admin-add" disabled={saving}>{saving ? "Đang lưu…" : "Lưu"}</button></form>}<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>{product ? "Sản phẩm" : "Thành viên"}</th><th>{product ? "Giá" : "Vai trò"}</th><th></th></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td><div className="admin-table-item"><span className="admin-table-icon"><Icon size={15} /></span><div><strong>{item.name}</strong><div className="admin-table-sub">{product ? item.description : item.bio}</div></div></div></td><td>{product ? item.price : item.title}</td><td><button className="icon-btn" onClick={() => edit(item)}><Pencil size={15} /></button><button className="icon-btn icon-danger" onClick={() => remove(item.id)}><Trash2 size={15} /></button></td></tr>)}</tbody></table></div></div>; }
