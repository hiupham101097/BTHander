import React, { useEffect, useState } from "react";
import { FileText, Pencil, Plus, Trash2, X } from "lucide-react";
const blank = { team_member_id: "", title: "", excerpt: "", content: "", status: "published" };
export default function ArticlesManager() { const [articles, setArticles] = useState([]), [members, setMembers] = useState([]), [form, setForm] = useState(blank), [editing, setEditing] = useState(null), [open, setOpen] = useState(false), [error, setError] = useState(""); const load = () => { fetch("/api/articles", { credentials: "include" }).then((r) => r.json()).then((b) => setArticles(b.data || [])); fetch("/api/team", { credentials: "include" }).then((r) => r.json()).then((b) => setMembers(b.data || [])); }; useEffect(() => { load(); }, []); const update = (field) => (e) => setForm((v) => ({ ...v, [field]: e.target.value })); const save = async (e) => { e.preventDefault(); const response = await fetch(editing ? `/api/articles/${editing}` : "/api/articles", { method: editing ? "PATCH" : "POST", credentials: "include", headers: { "content-type": "application/json" }, body: JSON.stringify({ ...form, team_member_id: Number(form.team_member_id) }) }); const body = await response.json(); if (!response.ok) return setError(body.error || body.errors?.[0]); setOpen(false); load(); }; const edit = (article) => { setEditing(article.id); setForm({ team_member_id: String(article.team_member_id), title: article.title, excerpt: article.excerpt || "", content: article.content, status: article.status }); setOpen(true); }; return <div><div className="admin-toolbar"><h2 className="admin-section-title">Bài viết chia sẻ ({articles.length})</h2><button className="btn-primary admin-add" onClick={() => { setEditing(null); setForm(blank); setOpen(true); }}><Plus size={17} />Viết bài mới</button></div>{open && <div className="admin-modal-backdrop" onMouseDown={() => setOpen(false)}><form className="admin-form admin-modal" onSubmit={save} onMouseDown={(e) => e.stopPropagation()}><div className="admin-form-header"><h3>{editing ? "Chỉnh sửa bài viết" : "Bài viết mới"}</h3><button className="icon-btn" type="button" onClick={() => setOpen(false)}><X size={17} /></button></div>{error && <p className="form-error">{error}</p>}<div className="admin-form-grid"><label>Tác giả<select required value={form.team_member_id} onChange={update("team_member_id")}><option value="">Chọn thành viên</option>{members.map((member) => <option value={member.id} key={member.id}>{member.name}</option>)}</select></label><label>Trạng thái<select value={form.status} onChange={update("status")}><option value="published">Công khai</option><option value="draft">Bản nháp</option></select></label><label className="admin-form-full">Tiêu đề<input required value={form.title} onChange={update("title")} /></label><label className="admin-form-full">Tóm tắt<textarea rows="3" value={form.excerpt} onChange={update("excerpt")} /></label><label className="admin-form-full">Nội dung bài viết<textarea required rows="12" value={form.content} onChange={update("content")} /></label></div><button className="btn-primary admin-add">Lưu bài viết</button></form></div>}<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Bài viết</th><th>Tác giả</th><th>Trạng thái</th><th></th></tr></thead><tbody>{articles.map((article) => <tr key={article.id}><td><div className="admin-table-item"><span className="admin-table-icon"><FileText size={15} /></span><div><strong>{article.title}</strong><div className="admin-table-sub">{article.excerpt}</div></div></div></td><td>{article.member_name}</td><td>{article.status}</td><td><button className="icon-btn" onClick={() => edit(article)}><Pencil size={15} /></button><button className="icon-btn icon-danger" onClick={async () => { await fetch(`/api/articles/${article.id}`, { method: "DELETE", credentials: "include" }); load(); }}><Trash2 size={15} /></button></td></tr>)}</tbody></table></div></div>; }
