import React, { useEffect, useState } from "react";
import { ArrowLeft, BriefcaseBusiness, Code2, FileText, UserRound } from "lucide-react";
import { Link, useParams } from "react-router-dom";

function List({ title, icon: Icon, items }) { if (!items?.length) return null; return <section className="member-section"><h2><Icon size={20} />{title}</h2><div className="member-list">{items.map((item, index) => <article key={index}>{item}</article>)}</div></section>; }
export default function TeamProfile() { const { id } = useParams(); const [member, setMember] = useState(null); useEffect(() => { fetch(`/api/team/${id}`).then((r) => r.ok ? r.json() : null).then((body) => setMember(body?.data || null)); }, [id]); if (!member) return <div className="wrap detail-state">Đang tải hồ sơ thành viên…</div>; return <div className="member-profile"><div className="wrap member-back"><Link to="/#team" className="detail-back"><ArrowLeft size={15} />Quay lại đội ngũ</Link></div><header className="member-hero"><div className="wrap member-hero-inner"><div className="member-avatar">{member.avatar_url ? <img src={member.avatar_url} alt={member.name} /> : <UserRound size={46} />}</div><div><p>THÀNH VIÊN BTHANDER</p><h1>{member.name}</h1><span>{member.title}</span></div></div></header><main className="wrap member-content"><section className="member-intro"><h2>Giới thiệu</h2><p>{member.profile_intro || member.bio || "Thông tin đang được cập nhật."}</p></section><List title="Kinh nghiệm qua từng giai đoạn" icon={BriefcaseBusiness} items={member.experience} /><List title="Ngôn ngữ & chuyên môn" icon={Code2} items={member.skills} /><List title="Dự án tiêu biểu" icon={BriefcaseBusiness} items={member.featured_projects} /><Link className="member-articles-link" to={`/team/${member.id}/articles`}><FileText size={20} /><span><strong>Góc chia sẻ kinh nghiệm</strong><small>Đọc các bài viết và góc nhìn chuyên môn của {member.name}.</small></span></Link></main></div>; }
